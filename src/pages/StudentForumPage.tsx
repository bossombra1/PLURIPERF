import React,{useEffect,useState}from'react';import{Link}from'react-router-dom';import{useAuth}from'../context/AuthContext';import{api}from'../api/client';import{Logo}from'../components/Logo';import{Alert,AlertDescription}from'../components/ui/alert';import{MessagesSquare,ArrowLeft,Loader2}from'lucide-react';
type Topic={id:number;title:string;body:string;author:string;created_at:string};
export const StudentForumPage:React.FC<{lang?:'fr'|'en'}>=()=>{const{user}=useAuth();const[topics,setTopics]=useState<Topic[]>([]);const[title,setTitle]=useState('');const[body,setBody]=useState('');const[loading,setLoading]=useState(true);const[sending,setSending]=useState(false);const[error,setError]=useState('');const[success,setSuccess]=useState('');const load=()=>{if(!user)return;setLoading(true);api.get<Topic[]>('/forum').then(setTopics).catch(e=>setError(e.message||'Erreur')).finally(()=>setLoading(false))};useEffect(load,[user]);const create=async(e:React.FormEvent)=>{e.preventDefault();setSending(true);setError('');try{await api.post('/forum',{title,body});setTitle('');setBody('');setSuccess('Sujet publié.');load()}catch(e:any){setError(e.message||'Erreur')}finally{setSending(false)}};if(!user)return <div className="min-h-[70vh] flex items-center justify-center py-12 px-4"><Alert variant="destructive" className="max-w-md"><AlertDescription>Authentification requise.</AlertDescription></Alert></div>;return <div className="min-h-[70vh] py-12 px-4"><div className="max-w-4xl mx-auto space-y-8"><div className="flex items-center justify-between"><div className="space-y-2"><Link to="/espace-etudiant" className="text-sm text-primary hover:text-primary-hover flex items-center gap-1"><ArrowLeft className="h-4 w-4"/>Retour au tableau de bord</Link><h1 className="text-3xl font-bold text-text-primary">Forum</h1></div><Logo variant="emblem-only" size="sm"/></div>{error&&<Alert variant="destructive"><AlertDescription>{error}</AlertDescription></Alert>}{success&&<Alert><AlertDescription>{success}</AlertDescription></Alert>}<div className="grid grid-cols-1 md:grid-cols-2 gap-6"><div className="bg-surface border border-border-subtle rounded-[calc(var(--radius)+4px)] p-6"><h2 className="font-semibold flex items-center gap-2 mb-4"><MessagesSquare className="h-5 w-5 text-primary"/>Sujets</h2>{loading?<Loader2 className="h-6 w-6 animate-spin text-primary"/>:topics.length===0?<p className="text-text-muted text-sm">Aucun sujet.</p>:<div className="space-y-4">{topics.map(t=><article key={t.id} className="border-b border-border-subtle pb-4"><h3 className="font-semibold">{t.title}</h3><p className="text-xs text-text-muted">Par {t.author||'Utilisateur'} · {new Date(t.created_at).toLocaleString('fr-FR')}</p><p className="text-sm text-text-secondary mt-2">{t.body}</p></article>)}</div>}</div><form onSubmit={create} className="bg-surface border border-border-subtle rounded-[calc(var(--radius)+4px)] p-6 space-y-4"><h2 className="font-semibold">Créer un sujet</h2><input required minLength={3} className="w-full border border-border-subtle rounded-md p-3 bg-background" value={title} onChange={e=>setTitle(e.target.value)} placeholder="Titre"/><textarea className="w-full border border-border-subtle rounded-md p-3 bg-background min-h-32" value={body} onChange={e=>setBody(e.target.value)} placeholder="Votre message"/><button disabled={sending} className="px-4 py-2 bg-primary text-white rounded-md">{sending?'Publication...':'Publier'}</button></form></div></div></div>};